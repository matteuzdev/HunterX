{
  "name": "hunterx-lead-intelligence",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check lib/engine.js && node --check lib/http.js && node --check api/health.js && node --check api/search.js && node --check api/enrich.js"
  },
  "engines": {
    "node": ">=20"
  }
}
