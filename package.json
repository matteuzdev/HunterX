{
  "name": "hunterx-lead-intelligence",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node app.js",
    "start": "node app.js",
    "check": "node --check app.js && node --check client.js && node --check lib/engine.js && node --check lib/http.js && node --check api/health.js && node --check api/search.js && node --check api/enrich.js"
  },
  "engines": {
    "node": "22.x"
  }
}
